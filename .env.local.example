# Local dev overrides — copy to .env.local (gitignored).
# Run scripts with: uv run --env-file=.env.local path_to_script.py
# These values are injected as env vars by uv, taking priority over .env.

PYTHONPATH=src
POSTGRES_HOST=localhost
